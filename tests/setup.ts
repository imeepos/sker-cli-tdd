/**
 * Jest 测试设置文件
 * 此文件在每个测试文件之前执行
 * 遵循 TDD 原则：首先设置测试环境
 */

// 全局测试配置
beforeEach(() => {
  // 在每个测试前重置任何全局状态
  // 这确保了 TDD 原则要求的测试独立性
});

afterEach(() => {
  // 每个测试后清理
  // 确保测试用例之间没有污染
});

// 可以在这里添加自定义匹配器或全局测试工具
// 遵循 TDD 原则：让测试清晰且富有表现力

export {};
